[package]
name = "fournever_core"
version = "0.1.0"
edition = "2021"
description = "Pure-logic core for 4neverCompanyOS (no Tauri dependency; unit-testable anywhere)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
