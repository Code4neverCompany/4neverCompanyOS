# Track stable to match CI's dtolnay/rust-toolchain@stable.
# Prevents rustfmt version drift between local and CI (root cause of NEVAAA-112).
[toolchain]
channel = "stable"
profile = "default"
components = ["rustfmt", "clippy"]
targets = ["x86_64-pc-windows-msvc"]
