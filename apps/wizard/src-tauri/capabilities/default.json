{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "First-run wizard default capabilities — file picker + workspace commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
