{
  "name": "@c4n/workflow-engine",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "BMAD YAML workflow executor (D-12). Reads workflows from _bmad/, runs phases sequentially with user-approval gates, dispatches dynamic personas.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@c4n/core": "workspace:*",
    "@c4n/progress-signal": "workspace:*",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "typescript": "5.8.3",
    "vitest": "^2.1.8"
  }
}
