{
  "name": "@c4n/telemetry",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Per-persona token-cost telemetry. Parses each CLI's structured output (Claude Code, agy, Hermes) and persists token counts to workspace SQLite.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@c4n/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "^2.1.8"
  }
}