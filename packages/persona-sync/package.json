{
  "name": "@c4n/persona-sync",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Bidirectional sync between canonical persona files (vault) and CLI-specific tool configs (claude.md / agy.md / agent.md). Last-writer-wins + per-persona conflict log.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@c4n/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "^2.1.8"
  }
}