{
  "name": "@c4n/supermemory-client",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Opt-in Supermemory wrapper for cross-project semantic memory. Per-category toggles drive what crosses the local-to-cloud boundary.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@c4n/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "^2.1.8"
  }
}
