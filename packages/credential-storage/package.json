{
  "name": "@c4n/credential-storage",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Thin TS facade over the OS keychain (Windows Credential Manager / macOS Keychain / Linux Secret Service). The actual keychain work is in crates/credential-storage (TBD — Rust side).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@c4n/core": "workspace:*",
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "^2.1.8"
  }
}
